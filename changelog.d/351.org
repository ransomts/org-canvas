* Added
- #351 A grading file carries each student's document processor reports: the pull and every refresh read them in one GraphQL query per column (=ltiAssetReportsConnection=) and write =SIMILARITY= and =AI_WRITING= on the heading (=33%=, or =pending=, =failed=, =not processed=; any other report type as =REPORT_<TYPE>=), with a =Reports: N processed, N failed, N pending= line under the =Assignment:= line and in the summary table. =org-canvas-submissions-status= adds the same line summed over the columns, naming the ones where a report failed. A column without a processor gets nothing, and a failed read is one warning while the pull goes on without the reports.
