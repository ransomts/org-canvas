#+TITLE: Changelog
#+OPTIONS: toc:nil num:nil

All notable changes to org-canvas are recorded here, newest first.
Each entry names the pull request that merged it.

* Unreleased

** Added

- =org-canvas-pull-people=: the course roster, pulled into =people.org= as one heading per person under Students, Teachers, TAs, Observers and Designers, with role, sections (linked to sections.org), enrollment state and last activity; the SIS and login ids are opt-in and a grade or an email is never written. Canvas's Test Student is skipped, a person in two sections is one heading, and a re-pull matches on the user id wherever the heading sits
- =org-canvas-api-request-all-pages= follows the Link header: Canvas pages the enrollments endpoint by bookmark and answers a numbered second page with a 400, so a course with more than a hundred enrollments could not be listed at all; every paginated endpoint now walks =rel="next"=, with numbered pages kept only as the fallback for a reply without headers
- =:pull-only= on a registered property: a value a pull writes and no push reads. =org-canvas-validate= no longer warns that such a timestamp is in the past (announcements' and pulled replies' =POSTED_AT= and =AUTHOR=), and the manual marks it as Canvas's to set
- #202 Grade post policies and posting grades: =POST_POLICY= on the course heading in settings.org and, when it differs from the course's, on an assignment heading, pulled and pushed through Canvas's GraphQL endpoint; =org-canvas-submissions-post-grades= (=P= in a grading file) posts an assignment's grades, the push offers it under a manual policy, and each student carries =POSTED_AT=. A small GraphQL transport (=org-canvas--graphql-query=, =org-canvas--graphql-mutate=) with the read-only guard and the dry run
- =org-canvas-pull-groups=: the groups inside each group category and the students in each group, pulled into =groups.org= (categories at level 1, linked to =group-categories.org=; groups at level 2 with their member limit, join level and member count; members as a list of names). Pull-only, and the file holds student names, so keep it out of a course repository as the submissions directory is
- =org-canvas-pull-grading-periods=: the course's grading periods are pulled into =grading-periods.org= (start, end and close dates, weight, closed flag), read-only since periods belong to the term; =org-canvas-validate= then warns when an assignment's =DUE_AT= falls outside every pulled period
- =org-canvas-pull-discussion-replies=: a pull-only archive of what was written under each discussion topic, into =discussion-replies.org= — one heading per topic with entries, its entries and their replies beneath, matched by =CANVAS_ENTRY_ID= on a re-pull; nothing local is deleted and nothing is written to Canvas
- =documentation/architecture/coverage.org=: the map of what org-canvas covers in Canvas, what it touches in one direction only, and what it never reads or writes, with the gaps most likely to matter for a course and the grep that refreshes it
- =org-canvas-sync-file-at-point=: files are on the sync macro, so the file at point can be uploaded alone. The macro gains =:hash= (a function replacing the payload's md5, or =push= when the push owns change detection and answers =skip= for nothing to send) and =:dry-run push= (the push guards its own writes and previews them); a parser that declines a heading marks it a skip
- =org-canvas-sync-outcome-at-point=, =org-canvas-sync-outcome-group-at-point= and =org-canvas-sync-outcome-groups=: outcomes are on the sync macro, one =org-canvas-define-sync= per heading level, so each level has the push at point every other content type has. The macro gains =:prepare=, a function run once before the first entry whose result the run keeps (the root outcome group), and =:first=, a sync the generated command runs beforehand (groups before outcomes)
- #184 =DOCUMENT_PROCESSOR= on assignments: the Turnitin document processor attached in the web UI is written by pull, shown by the drift report whether or not the heading carries it, never sent by a push, and flagged by validate when typed ahead of the first sync; Canvas attaches one only in a browser, so org-canvas observes and protects it rather than setting it
- #179 (follow-up) Every remaining create path adopts a twin before it POSTs: classic quiz questions and New Quiz items match an unclaimed item of their name in the parent's list, New Quizzes ask the same adopt/skip/create question as the generic guard, outcomes and outcome groups search their parent before creating, the generic 404 recovery updates the item that still carries the title instead of POSTing a second, and classic quizzes, discussions, announcements and assignment groups gain a =:find-fn= so a push at point is guarded outside a sync's snapshot
- #164 Duplicate-title detection for pages, assignments, quizzes, new quizzes, rubrics, discussions, announcements and modules: the offline validator groups titles once =(OLD)=, semester stamps and Canvas's =(n)= suffixes are stripped, raising a twin that still carries a =DUE_AT= to an error; =org-canvas-pull-all= names the types whose titles collapse
- #163 =org-canvas-read-only=: mark an adopted course read-only in its credentials file and every write is refused at the transport, the menu's push half greys out, and =org-canvas-init= asks whether the course is yours
- #127 Submissions: completion rule, =DAYS_LATE=, notes that survive pulls, and =EX= for excused grades
- #126 Submissions: draft comments in the grading file and post them with =S=
- #119 =RUBRIC_USE_FOR_GRADING= and =RUBRIC_HIDE_SCORE_TOTAL= on assignments
- #117 Submissions: link the assignment's rubric and list its criteria
- #116 Submissions: link the assignment, SpeedGrader, and local attachments
- #115 Submissions: grading files that survive the session
- #110 Drift report rows have verbs: visit, acknowledge, delete, and pull
- #109 Drift report recognizes media that course content embeds instead of listing it as extra
- #108 Module items moved between modules are reconciled instead of duplicated
- #107 =org-canvas-adopt-at-point= adopts the Canvas item that shares a heading's title
- #100 Known-extras and excluded-features valves let a synced course read zero divergences
- #90 Calendar events register as a feature: drift checks, duplicate guard, and pull-at-point
- #88 External-tool (LTI) assignments such as Gradescope; skipped items are reported by name; pull includes the front page
- #80 =org-canvas-files-force-reupload= re-uploads when the hash cannot see the difference
- #69 =org-canvas-pull-at-point= accepts one Canvas-side edit without pulling the whole file
- #64 A module item may carry its own =PUBLISH_AT=, ahead of its module's
- #58 Publish a module and everything it lists, on demand or on a =PUBLISH_AT= schedule
- #57 =org-canvas-diff=, a read-only drift report between the Org files and Canvas
- #41 Report Canvas assignment groups the org file does not manage
- #40 Warn when assignment group weights do not sum to 100
- #32 =org-canvas-prune-FEATURE= commands delete Canvas items the org file no longer lists
- #31 Conflict resolution can pull a module, items included
- #21 End-of-run retry pass for module items whose targets synced later in the run
- #20 Aggregated global sync summary with item-level skips
- #19 Validation rule for front-page and published state; the drop-rules check runs again

** Changed

- =org-canvas-sync-files= runs on the shared pipeline: the same context, counters, summary and orphan warnings as every other content type, and the drift snapshot fetched once per run. The upload tiers, their fifteen dry-run guards and the re-upload rules are unchanged; only the loop around them went. The closing line reads "Files sync: N success, N skipped, N failed" and a dry run "Files dry-run: N would sync", as for other types; folder headings count as skips as before
- The sync and delete runners take one spec plist each instead of eleven positional or keyword arguments: =org-canvas-define-sync= builds a sync spec (=org-canvas--sync-spec-keys=) for =org-canvas--sync-run-pipeline= and =org-canvas--push-at-point-runtime=, =org-canvas-define-delete-all= a delete spec (=org-canvas--delete-spec-keys=) for the delete-all, prune and delete-items runtimes; an unknown key is an error rather than a silently ignored option. Generated commands are unchanged; a module that called a runner by hand (outcomes) names its arguments now
- =org-canvas-core-org.el= is three files: the Org buffer, batch-file, timestamp and link-resolution helpers stay; HTML export and HTML-to-Org conversion are =org-canvas-core-html.el=; the pull helpers, the file-URL rewriter, =org-canvas-define-pull=, =org-canvas-define-pull-item= and the pull summary are =org-canvas-core-pull.el=. No function changed; =org-canvas-core= still requires everything, and the test suite is split the same way. =test/org-canvas-core-sync-test.el= likewise sheds the specs that belong to =core-delete= and =core-conflict= into files of their own, and the pull, org, html, api and config specs it carried into theirs
- #155 A 403 your Canvas role cannot do anything about is reported as a permission skip, named in the closing line and recorded in the pull summary, instead of counted as a failure; the summary prints to stdout under =--batch=
- #156 Pagination progress names the resource and speaks only when a fetch really is paging; the first page is logged, not announced
- #141 Sync run state (batch conflict answers, the module's pull function, the moved/pending/changed id lists) lives in a per-run context instead of dynamically bound globals, so nothing one run decides reaches the next
- #142 Every pause a sync takes goes through =org-canvas--wait=, which counts down in the echo area and keeps the display alive; the settings pull says so when the late policy or navigation tabs cannot be read; =org-canvas.el= split into status, publish, adopt, and orphans command files
- #140 Emacs 29.1 is the minimum (was 28.1); obsolete =when-let= replaced; a clean compile is warning-free; this changelog
- #92 Documentation brought up to date with issues #48, #67, and #81 to #87
- #91 Docstring first lines kept imperative for checkdoc on Emacs 30
- #76 The straight.el recipe names =lisp/*.el=
- #46 The weekly mutation job gates on the survivor baseline
- #45 Mutation baseline populated, harness fixed, suite de-flaked
- #42 Mutation testing gates on a baseline instead of a score
- #39 Override handling gains tests that kill two mutation survivors
- #33 The test suite refuses unmocked HTTP calls
- #29 File-id rotation repair relies on the items digest; hash refreshed after a heal
- #24 Demo course uses direct file links for File module items
- #18 Canvas error messages surface in the log; drop-rule rejections defer instead of failing; duplicate log lines removed
- #2 Security hardening, pull-side contract tests, and complete property docs
- #1 Robustness fixes and deep test infrastructure (contract, mutation, round-trip, SSOT)

** Fixed

- The pull menu bound =G= twice, to group categories and to grading periods; grading periods are =y= and the roster is =u=
- #198 A module item's =COMPLETION_REQUIREMENT= and =MIN_SCORE= reach Canvas: the JSON payload carried them as bracketed form-encoded keys, which Canvas ignored without a word, so no requirement was ever set
- #199 =org-canvas-pull-modules= links a Page item to its pages.org heading (Canvas names a page by =page_url=, not =content_id=) and writes =COMPLETION_REQUIREMENT= and =MIN_SCORE= back instead of dropping them
- #196 A sync no longer warns that every id below its own heading level may be orphaned: the orphan and duplicate-id checks read the headings the sync's query selects, so a modules run stops reporting each module item, a quizzes run each question, and the outcomes passes each other's level
- #188 A second push at point in one batch buffer no longer refuses its stamp when a sync client restamped the file's modification time after the first push's save: the batch guard now compares the file's content with what the buffer last read or wrote, and treats a moved time over unchanged text as a restamp rather than a dual-buffer clobber
- #189 The submissions completion rule checks the buffer before it prompts for the points, and its body is instrumented again (a sexp =interactive= form had hidden it from coverage); the rubric criterion outcome warning names the criterion instead of an empty string; a File module item with no title of its own is described by its files.org heading's link description rather than the whole link. Forty-eight specs cover the branches the suite had never reached; =SECURITY.md=, a bug report template and a pull request checklist join the repository
- #178 The bearer token is no longer an argument on the request path, so a backtrace of an api-error escaping a batch script cannot print it: the headers are resolved inside the transport function, request logging never reads the token, and plz's own frames are unwound before the error reaches a debugger
- #158 A nested =files.org= can be re-pulled: files are matched by Canvas id wherever they sit, a file whose folder changed is moved rather than duplicated, and hand-written content survives; downloads skip a local copy that is already the right size
- #153 Navigation tabs are pulled again: Canvas sends a JSON array, which =json-read= decodes as a vector, and the formatter walked it with =dolist=
- #157 An optional require is optional again: a feature that is on =load-path= but raises while loading no longer aborts loading org-canvas, and says what was lost
- #152 Unwrap plz's error datum: it signals =plz-http-error= with a list, not a bare struct, so session cookies escaped masking, Canvas's own error text was replaced by a printed struct, and 429 and 5xx were never retried on the primary request path
- #154 Redact on the way to the user, not only to the log: error text sent through =message= or stored in the pull summary reached stdout unmasked

- #132 Drift report reads an excluded feature's bodies for the media they embed
- #131 Baseline re-read after a post-fn (rubric association) writes to Canvas, ending phantom drift
- #130 Rubrics update in place instead of being deleted and re-created, keeping associations and assessments
- #129 Batch runs no longer ask the supersession question
- #128 =DAYS_LATE= only on rows that were actually submitted
- #122 Rubric data folded into the assignment change hash
- #113 Submissions send each =include[]= separately and fall back to =user_id=
- #106 =#+LAST_SYNCED= advances on any successful stamp, not only full syncs
- #99 Course files open by truename; stale buffers survive in batch
- #96 =ALL_DAY= is not compared on multi-day calendar events
- #95 File drift is decided from =modified_at=, the content timestamp
- #89 Drift report compares bodies; a dry run previews conflicts; duplicate titles are guarded before a create
- #78 A replaced file is overwritten in place instead of deleted first
- #75 A pre-split file hash is verified against Canvas instead of re-uploaded
- #74 Uploaded files no longer start with a stray CRLF
- #73 An unattended sync resolves a conflict via =org-canvas-conflict-strategy= instead of dying
- #68 The global sync summary describes the run that happened (dry-run, conflict, and pulled columns)
- #65 Drift report reads nested Canvas fields via =:remote-fn= instead of flagging every item
- #60 A classic quiz publishes only once its questions exist
- #56 Files that only changed metadata are updated, not re-uploaded, and checked for conflicts
- #55 Conflict detection actually runs, and Canvas is checked before a skip
- #54 File =PUBLISHED= maps to =locked= and applies after the upload
- #53 Module sync no longer publishes the content its items point at
- #35 Dry runs never write to Canvas; stranded upload ids are recovered
- #30 Question and item digests are folded into quiz payload hashes
- #28 Late-policy updates use PATCH; PUT is not routed and always 404s
- #27 Module items are folded into =PAYLOAD_HASH= so item edits trigger a sync
- #25 Real PATCH support via a direct curl fallback (New Quizzes updates)
- #23 Unchanged file uploads are skipped; modules are repaired when file ids rotate
- #16 No false "Unresolved link" warnings for property-drawer links
- #14 Unsupported HTTP methods are rejected up front instead of degrading to a bodyless GET
- #12 Session cookies no longer leak into sync logs
